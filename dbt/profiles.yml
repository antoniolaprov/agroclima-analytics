agroclima:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "../data/warehouse.duckdb"
      threads: 1  # fixado em 1: build multi-thread trava no encerramento do pool do DuckDB no Windows
